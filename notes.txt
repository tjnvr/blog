* markdownSourcePath could be used to determine -> outputHTMLFilePath, pageSection, a dedicated package should guide the squeleton of this part for the whole site
    - take in argument contentDir, assets, scriptsDir and their associated outDir
    - expose : SectionExtractor ExtractSection(markdownSourcePath)
